import numpy as np
from numpy.typing import NDArray

def extract_features(window: NDArray[np.float64]) -> NDArray:
    """
    Extracts features from a window of time series data.

    :param window: Array-like time series window
    :return: Array of extracted features (e.g., mean, std)
    """
    pass


